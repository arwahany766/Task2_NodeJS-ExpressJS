<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Register - Student System</title>
    <link rel="stylesheet" href="/css/register.css" />
  </head>
  <body>
    <div class="register-container">
      <div class="form-section">
        <h2>Create Account</h2>

        <% if (locals.error_msg && locals.error_msg.length > 0) { %>
        <div class="alert alert-error"><%= error_msg %></div>
        <% } %>

        <form action="/auth/register" method="POST">
          <label for="username">Username</label>
          <input type="text" id="username" name="username" required />

          <label for="email">Email</label>
          <input type="email" id="email" name="email" required />

          <label for="password">Password</label>
          <input type="password" id="password" name="password" required />

          <label for="confirmPassword">Confirm Password</label>
          <input type="password" id="confirmPassword" name="confirmPassword" required />

          <button type="submit" class="btn">Register</button>
        </form>

        <p class="switch-link">Already have an account? <a href="/auth/login">Login</a></p>
      </div>

      <div class="info-section">
        <h1>Student Management</h1>
        <p>Join now to easily manage all student records, track progress, and handle data efficiently.</p>
      </div>
    </div>
  </body>
</html>
